version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: crabflow
      POSTGRES_PASSWORD: crabflow
    ports:
    - 5432:5432

  registry:
    image: registry:2
    ports:
    - 5000:5000
