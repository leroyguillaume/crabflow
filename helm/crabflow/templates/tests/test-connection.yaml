apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crabflow.fullname" . }}-test-connection"
  labels:
    {{- include "crabflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crabflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
