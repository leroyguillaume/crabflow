apiVersion: v1
kind: Service
metadata:
  name: {{ include "crabflow.fullname" . }}
  labels:
    {{- include "crabflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crabflow.selectorLabels" . | nindent 4 }}
